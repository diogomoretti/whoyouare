---
layout: src/templates/layouts/default.hbs
---

<div class="header-internal" {{#if page.bg-header }} style="background-image: url({{ page.bg-header }});" {{/if}}>
    <h1 class="header-title internal">{{ page.title }}</h1>
    {{#if page.description }}<p class="header-description">{{ page.description }}</p>{{/if}}
</div>

<div class="internal-box">
    <div class="container">
        {{> body}}
    </div>
</div>